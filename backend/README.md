# Backend (Node.js + TypeScript) - Starter

This folder contains a minimal Express + TypeScript + Mongoose starter for local development.

Quick start:

```bash
cd backend
npm install
npm run dev
```

Environment: copy `.env.example` to `.env` and set `MONGO_URI`, `JWT_SECRET`, and `CLIENT_URL`.
The backend will not start without `MONGO_URI`.
